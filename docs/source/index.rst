Voxel game engine
=================

Rust + Vulkan workspace. See the repository ``README.md`` and ``agents.md`` for build steps and architecture.

Install Sphinx dependencies from ``docs/requirements.txt``, then from ``docs/source`` run ``sphinx-build -b html . ../_build``.
